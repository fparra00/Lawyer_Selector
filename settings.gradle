pluginManagement {
    repositories {
        google()

        gradlePluginPortal()
        mavenCentral()
        flatDir{
            dirs 'app/libs'
        }

    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        flatDir{
            dirs 'app/libs'
        }

    }
}
rootProject.name = "LawyerSelector.v2"
include ':app'
include ':ej'
